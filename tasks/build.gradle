apply plugin: 'groovy'

dependencies {
  compile (
    gradleApi(),
    localGroovy()
  )

  testCompile (
    'org.spockframework:spock-core:0.6-groovy-1.8'
  )
}

test.testLogging {
  exceptionFormat "full"
  //showStandardStreams true
}